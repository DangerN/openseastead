import {Container} from "@mui/material";
import Markdown from "../components/Markdown";

function SeasteadModule() {
    return <Container>
        <Markdown url={'assets/markdown/seastead-module.md'}/>
    </Container>
}

export default SeasteadModule
